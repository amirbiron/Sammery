services:
  - type: web
    name: Sammery
    # ------------------------------------------------------------------
    # השינוי המרכזי: הגדרת סביבת הריצה ל-Docker
    # Render יחפש כעת את קובץ ה-Dockerfile שבפרויקט ויבנה לפיו.
    env: docker
    # ------------------------------------------------------------------
    plan: free
    healthCheckPath: /
    envVars:
      - fromGroup: sammery-secrets
